apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ocp-service-account-cluster-role
rules:
  - apiGroups: [""]
    resources:
      - namespaces
      - projects
      - projectrequests
    verbs:
      - create
      - patch
      - delete